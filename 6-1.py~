import re
input = """turn on 0,0 through 759,964
turn off 8,8 through 871,914
turn off 0,0 through 929,502
turn on 0,0 through 977,877""".split('\n')
grid = [[False for x in range(9)] for x in range(9)]

def turnOn(x, y):
	grid[x][y] = True

def turnOff(x, y):
	grid[x][y] = False

def toggle(x, y):
	grid[x][y] = not grid[x][y]

for item in input:
	f = turnOn
	if item.startswith("turn off"):
		f = turnOff
	if item.startswith("toggle"):
		f = toggle
	cordinates = re.findall('\d+,\d+', item)
	x = int(cordinates[0].split(',')[0])
	y = int(cordinates[0].split(',')[1])
	x1 = int(cordinates[1].split(',')[0])
	y1 = int(cordinates[1].split(',')[1])
	print x, y, x1, y1
	
	f(x, y)
print grid

